# 流式输出功能测试指南

## 修复内容

### 问题诊断
uni-app 的 `uni.request` 不支持流式接收 SSE 数据,需要改用 `XMLHttpRequest`

### 修复方案

#### 1. 前端 API 层修复 (`frontend/api/conversation.js`)

**修改前**: 使用 `uni.request` 一次性接收所有数据
```javascript
uni.request({
  url: url,
  method: 'POST',
  success: (res) => {
    // res.data 是完整的响应,无法流式处理
    const lines = res.data.split('\n')
    // ...
  }
})
```

**修改后**: 使用 `XMLHttpRequest` 实现真正的流式接收
```javascript
const xhr = new XMLHttpRequest()
xhr.open('POST', url, true)

xhr.onreadystatechange = function() {
  if (xhr.readyState === 3) {  // LOADING - 正在接收数据
    const newData = xhr.responseText.substring(buffer.length)
    buffer = xhr.responseText
    processSSEData(newData, onEvent)  // 实时处理新数据
  }
}
```

#### 2. 状态管理优化

添加了详细的调试日志:
```javascript
console.log('开始流式对话:', conversationId, content)
console.log('收到事件:', event, data)
console.log('AI 内容更新:', tempAiMessage.content)
```

#### 3. 样式类绑定优化

```vue
:class="[message.role, { streaming: message.streaming, pending: message.pending }]"
```

## 测试步骤

### 1. 启动后端服务

```bash
cd backend
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 2. 启动前端服务

使用 HBuilderX 或命令行启动前端项目

### 3. 测试流式输出

#### 测试场景 1: 正常流式输出
1. 打开对话页面
2. 发送一条消息: "你好"
3. 观察效果:
   - ✅ 用户消息立即显示
   - ✅ 用户消息显示"发送中..."状态
   - ✅ AI 消息逐字显示
   - ✅ AI 消息右侧有闪烁光标
   - ✅ 流式完成后光标消失

#### 测试场景 2: Markdown 渲染
1. 发送消息: "给我一个Python学习计划"
2. 观察效果:
   - ✅ 流式输出时显示纯文本
   - ✅ 流式完成后解析 Markdown
   - ✅ 标题、列表、代码块正确显示

#### 测试场景 3: 打开浏览器控制台
1. 按 F12 打开开发者工具
2. 查看 Console 标签
3. 发送消息后观察日志输出:
   ```
   开始流式对话: 1 "你好"
   临时消息已创建: {id: "temp-1741234567890", ...}
   收到事件: user_message {id: 1, ...}
   收到事件: ai_chunk {content: "你"}
   收到事件: ai_chunk {content: "好"}
   收到事件: ai_chunk {content: "！"}
   AI 内容更新: 你
   AI 内容更新: 你好
   AI 内容更新: 你好！
   收到事件: ai_complete {...}
   流式对话完成
   ```

## 网络请求检查

### 查看网络请求

1. 打开浏览器开发者工具
2. 切换到 Network 标签
3. 筛选 `XHR` 请求
4. 发送消息
5. 查看请求:
   - **URL**: `http://localhost:8000/api/conversations/1/chat/stream`
   - **Method**: POST
   - **Status**: 200
   - **Content-Type**: `text/event-stream`
   - **Response**: 应该看到 SSE 格式的数据流

### SSE 响应格式示例

```
event: user_message
data: {"id": 1, "role": "user", "content": "你好", ...}

event: ai_chunk
data: {"content": "你"}

event: ai_chunk
data: {"content": "好"}

event: ai_chunk
data: {"content": "！"}

event: ai_complete
data: {"id": 2, "role": "assistant", "content": "你好！", ...}
```

## 常见问题排查

### 问题 1: 用户消息不立即显示

**检查项**:
- [ ] `conversationStore.appendMessage()` 是否被调用
- [ ] `message.pending` 是否正确设置
- [ ] 样式类 `.pending` 是否生效

**调试**: 查看 Console 日志,确认 `临时消息已创建` 日志

### 问题 2: AI 回复不流式输出

**检查项**:
- [ ] 使用 `XMLHttpRequest` 而非 `uni.request`
- [ ] `xhr.readyState === 3` 是否触发
- [ ] `onEvent` 回调是否被调用

**调试**: 查看 Console 日志,确认 `收到事件: ai_chunk` 日志

### 问题 3: Markdown 不渲染

**检查项**:
- [ ] `message.useMarkdown` 是否为 true
- [ ] `MarkdownRenderer` 组件是否正确导入
- [ ] 流式完成后是否替换为完整消息

**调试**: 查看 Console 日志,确认 `AI 回复完成` 日志中的 `useMarkdown: true`

### 问题 4: 流式输出卡住

**检查项**:
- [ ] 后端日志是否正常
- [ ] SSE 响应是否正常发送
- [ ] 网络连接是否稳定

**调试**:
1. 查看后端日志
2. 查看 Network 标签的 Response
3. 查看 Console 错误日志

## 性能优化建议

### 1. 消息列表虚拟滚动
当消息超过 100 条时,考虑使用虚拟滚动组件

### 2. 防抖处理
避免用户快速连续发送消息

### 3. 取消机制
允许用户取消正在发送的消息

## 后续优化

1. **打字机效果**: 为流式输出添加更自然的打字动画
2. **代码高亮**: 支持代码块语法高亮
3. **表情符号**: 支持 Markdown 中的表情符号渲染
4. **图片渲染**: 支持 AI 回复中的图片链接
5. **数学公式**: 支持 LaTeX 数学公式渲染

---

**测试完成标准**:
- ✅ 用户消息立即显示
- ✅ AI 消息逐字显示
- ✅ Markdown 正确渲染
- ✅ 无 Console 错误
- ✅ 性能流畅无卡顿
